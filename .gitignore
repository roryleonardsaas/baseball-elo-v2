# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Regeneratable ELO computation caches (large; rebuilt on first run)
cache/elo_*.pkl

# Raw Retrosheet event-file downloads (re-downloaded on demand)
retrosheet_raw/

# Parsed Retrosheet caches: regenerated, and the synthetic player-ID registry is
# machine-local, so these are not tracked (unlike the stable Statcast parquets).
cache/retro_*.parquet

# OS / editor
.DS_Store
.idea/
.vscode/

# Claude Code local config
.claude/

# Streamlit secrets
.streamlit/secrets.toml

# Keep the small Statcast parquet caches so the app loads instantly:
# cache/statcast_*.parquet and cache/player_names.parquet ARE committed.
